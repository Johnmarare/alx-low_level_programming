#include "main.h"
/**
 * _isdigit- Entry point
 * @c: input
 * Return: 1 for true, 0 otherwise.
 */

int _isdigit(int c)
{
	if ((c >= 48) && (c <= 57))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
