#include <stdio.h>

/**
 * main- Entry point.
 *
 * Description: learning code showing the use of the printf() function
 *
 * Return: Always returns 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
